# Tables

Store exported tables here.

Naming rule:

`YYYY-MM-DD_table_main_dataset.csv`

Example:

- `2026-05-03_table_main_cifar10.csv`

Every table should be traceable to one or more run directories in `experiments/runs/`.
