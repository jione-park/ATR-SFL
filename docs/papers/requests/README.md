# Paper Requests Guide

Use this directory to capture batches of papers the user wants the agent to investigate.

Each request batch should answer:

- which papers were requested
- why they were requested
- what kind of output is expected
- whether the reviews are still pending or complete

Use the template at `docs/papers/_templates/request.md`.
