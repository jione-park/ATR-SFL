# Decision Notes Guide

Decision notes record durable choices that change how Hermes is run or interpreted.

Examples:

- which dataset becomes the first official benchmark
- which communication accounting method is canonical
- which split point range is in or out of scope
- when an old result set becomes obsolete

## File naming

Use `YYYYMMDD_short_decision_name.md`.
